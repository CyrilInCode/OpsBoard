server:
  port: 8082

spring:
  application:
    name: team-service
  datasource:
    url: jdbc:postgresql://localhost:5434/teams
    username: opsboard
    password: opsboard
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

